.profile-container
    .image-profile-container{style:"background-image: url(#{url_for(@user.cover)})"}
        =user_avatar(@user)
        -# Somos amigos? -> si(muestra el texto)    no -> (muestra el boton)
        -if @my_account
            =link_to edit_user_path(current_user), class: 'btn-facebook-primary no-decoration' do
                %i.fas.fa-edit.white-text
                Editar perfil
        -elsif @are_friends
            %p.amistad-enviada Solicitud de amistad enviada
        -else
            =button_to friendships_path(friend_id: @user.id), method: :post, class: 'btn-facebook-primary no-decoration' do
                %i.fas.fa-user-plus.white-text
                Agregar a mis amigos
    .info-profile-container.radius-10
        %h2.font-bold.dark-gray-text= @user.username
        %p.font-light.light-gray-text= @user.bio